/*
** EPITECH PROJECT, 2020
** R_Type
** File description:
** Entity
*/

#ifndef ENTITY_HPP_
#define ENTITY_HPP_

#include <iostream>
#include <vector>
#include <bitset>
#include <array>
#include <set>
#include <memory>
#include <unordered_map>

const std::size_t MAX_ENTITY = 500;
const std::size_t MAX_COMPONENT = 13;

using Entity = std::size_t;

using Signature = std::bitset<MAX_COMPONENT>;

#endif /* !ENTITY_HPP_ */
